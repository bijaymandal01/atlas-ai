version: "3.9"

services:

  n8n:
    image: docker.n8n.io/n8nio/n8n

    ports:
      - "5678:5678"

    env_file:
      - .env

    volumes:
      - ./n8n_data:/home/node/.n8n

  backend:
    build: ./backend

    ports:
      - "8000:8000"

    env_file:
      - .env